import { StaticPage, staticPageMetadata } from "@/components/static-page";

export const metadata = staticPageMetadata("corrections");

export default function Page() {
  return <StaticPage slug="corrections" />;
}
